# 加密解密工具 - 使用教程

## 目录
1. [快速入门](#快速入门)
2. [配置文件说明](#配置文件说明)
3. [单目录加密解密](#单目录加密解密)
4. [多目录加密解密](#多目录加密解密)
5. [增量加密解密](#增量加密解密)
6. [去重机制说明](#去重机制说明)
7. [冲突解决方案](#冲突解决方案)

---

## 快速入门

### 1. 准备配置文件
程序需要一个名为 `encrypt_config.yaml` 的配置文件。程序第一次运行时会自动生成一个默认配置文件。

### 2. 运行命令
```bash
# 加密模式
./encrypt -encrypt

# 解密模式
./encrypt -decrypt
```

---

## 配置文件说明

### 配置文件示例 `encrypt_config.yaml`

```yaml
password: "你的安全密码"                  # 必填，用于加密解密
encrypt_type: "aes"                     # 加密类型：aes/blowfish/xor
target_paths:                           # 需要加密的目录列表
  - "/path/to/your/directory1"
  - "/path/to/your/directory2"
obfuscate_suffix: ".dat"                # 加密文件后缀
obfuscate_name_length: 12               # 混淆名长度
map_filename: ".app_encrypt"            # 映射表文件名
lock_filename: ".encrypt.lock"          # 锁文件名
map_storage_path: "/path/to/store/map"  # 映射表存储路径
```

### 参数详细说明

| 参数 | 说明 | 可选值/示例 |
|------|------|------------|
| `password` | 密码，用于生成加密密钥 | 任意强密码，建议长度>8位 |
| `encrypt_type` | 加密算法 | `aes` (推荐)、`blowfish`、`xor` |
| `target_paths` | 要加密的目标目录路径列表 | 可填一个或多个绝对路径 |
| `obfuscate_suffix` | 加密文件后缀 | `.dat`、`.enc` 等 |
| `obfuscate_name_length` | 混淆文件名长度 | 建议 8-16 位 |
| `map_storage_path` | 映射表和盐值保存位置 | 建议与加密文件分开保存 |

---

## 单目录加密解密

### 场景说明
只需要加密一个目录及其子目录中的所有文件。

### 操作步骤

#### 1. 创建配置文件
```yaml
password: "mypassword123!@#"
encrypt_type: "aes"
target_paths:
  - "/workspace/my_documents"
obfuscate_suffix: ".dat"
obfuscate_name_length: 12
map_filename: ".app_encrypt"
lock_filename: ".encrypt.lock"
map_storage_path: "/workspace/tmp"
```

#### 2. 执行加密
```bash
cd /workspace
./encrypt -encrypt
```

#### 3. 加密效果
- 所有文件会被加密并重命名为随机字符串 + .dat 后缀
- 所有子目录会被重命名为随机的隐藏目录（以 . 开头）
- 原始文件和目录名被保存到映射表中
- 程序输出加密统计：
```
✅ 混淆目录: photos -> .abcd1234efgh
✅ 加密文件: report.txt -> .xyz789qwe123.dat
==================== 加密完成 ====================
已加密文件：32
已混淆目录：5
```

#### 4. 执行解密
```bash
./encrypt -decrypt
```

#### 5. 解密效果
- 所有文件解密并恢复原始文件名
- 所有目录恢复原始目录名
- 映射表文件自动删除
- 程序输出解密统计：
```
✅ 解密文件: .xyz789qwe123.dat -> report.txt
✅ 恢复目录: .abcd1234efgh -> photos
==================== 解密完成 ====================
已解密文件：32
已恢复目录：5
```

---

## 多目录加密解密

### 场景说明
需要同时加密多个独立的目录。

### 操作步骤

#### 1. 修改配置文件
在 `target_paths` 中加入多个目录：
```yaml
password: "mypassword123!@#"
encrypt_type: "aes"
target_paths:
  - "/workspace/documents"
  - "/workspace/photos"
  - "/workspace/projects"
obfuscate_suffix: ".dat"
obfuscate_name_length: 12
map_storage_path: "/workspace/tmp"
```

#### 2. 执行加密
```bash
./encrypt -encrypt
```
程序会依次处理每个配置的目录。

#### 3. 执行解密
```bash
./encrypt -decrypt
```
所有目录同时被解密和恢复。

---

## 增量加密解密

### 场景说明
1. 第一次加密一批文件
2. 之后在同一个目录中添加新文件
3. 再次加密 - 只加密新文件，不影响已有加密文件
4. 或者解密时，新增的未加密文件会保留

### 操作步骤

#### 1. 第一次加密（正常加密
```bash
./encrypt -encrypt
```

#### 2. 添加新文件
手动复制或创建新文件到已加密的目录中。

#### 3. 再次加密
```bash
./encrypt -encrypt
```

**程序行为：**
- 自动识别哪些是已加密文件（通过映射表和MD5指纹
- 自动识别哪些是新添加的未加密文件
- 只加密新文件，已加密文件保持不变

#### 4. 解密
```bash
./encrypt -decrypt
```
所有文件（包括增量文件）都能正确解密。

---

## 去重机制说明

### 去重功能
程序内置智能去重机制，避免重复文件被加密浪费空间。

### 去重原理
1. **MD5 指纹计算**：每个文件加密前计算其 MD5 哈希值
2. **同级目录去重**：只在**同一个目录**内检测重复文件
3. **自动删除**：如果发现同级目录中已有相同 MD5 的加密文件，新文件会被自动删除

### 代码说明
去重逻辑在 [`collectDirFiles`](file:///workspace/encrypt.go#L603) 和 [`encryptFiles`](file:///workspace/encrypt.go#L686) 函数中：
- 程序维护一个 `encryptedMd5` 映射表
- 按目录分组存储已加密文件的 MD5 值
- 新文件加密前检查同一目录是否已存在相同 MD5 文件
- 发现重复则删除新文件，统计去重数量

### 输出示例
```
⚠️ 同级目录中已存在相同MD5的文件，删除当前文件: duplicate.txt
✅ 删除重复文件: duplicate.txt
已删除重复文件：1
```

---

## 冲突解决方案

### 1️⃣ 嵌套目录冲突（已修复
#### 问题现象
之前版本中，嵌套目录加密后解密会出现目录名重复：
```
原始: level1/level2/level3
解密后: level1/level1/level2/level1/level2/level3
```

#### 原因分析
- 旧版在混淆目录时保存了完整相对路径
- 解密时路径拼接逻辑导致重复

#### 解决方案
- **代码修复**：在 [`obfuscateDirsBottomUp`](file:///workspace/encrypt.go#L524) 函数中，只保存当前目录名（`filepath.Base`）
- **恢复优化**：在 [`recoverDirs`](file:///workspace/encrypt.go#L1488) 函数中，按正确顺序恢复目录
- **结果**：三层嵌套完美解密，结构正确

### 2️⃣ 盐值管理冲突（已修复
#### 问题现象
解密时无法获取加密时用的盐值，导致无法生成正确密钥。

#### 解决方案
- 加密时同时保存两个文件：
  - `.app_encrypt` - 加密的映射表
  - `.app_encrypt.salt` - 独立的盐值文件
- 解密时优先读取独立盐值文件，避免鸡生蛋问题

### 3️⃣ 文件覆盖冲突
#### 解决方案
- 解密时如果目标文件已存在，先检查MD5
- 自动删除重复文件，避免覆盖

### 4️⃣ 锁文件冲突
#### 机制
程序启动时创建锁文件 `.encrypt.lock`
- 如果发现锁文件存在，报错退出
- 避免两个进程同时操作一个目录

---

## 常见问题

### Q: 忘记密码怎么办？
A: 没有密码或盐值无法解密！请务必妥善保管密码和映射表。

### Q: 可以只加密部分文件吗？
A: 当前版本会加密目录中所有文件。建议将要加密的文件单独放一个目录。

### Q: 不同加密算法可以混用吗？
A: 不可以！加密用什么算法，解密必须用相同算法。

### Q: 映射表丢了还能解密吗？
A: 不能！映射表包含原始文件名和目录名，必须备份好。

---

## 安全建议

1. **密码强度**：使用至少 12 位，包含大小写、数字、符号的强密码
2. **备份映射表**：映射表和盐值文件非常重要，建议离线备份
3. **算法选择**：推荐使用 AES，安全性最高
4. **分开存储**：映射表不要和加密文件放在一起
